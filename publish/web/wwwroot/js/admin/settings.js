/**
 * SmartBus Admin — Settings page module (handlers via SB.on).
 * Settings page has no page-specific state; everything runs through utils.js.
 */
(function () {
  'use strict';
  // All actions needed (toggle-switch, open-change-password, save-password)
  // are already registered by utils.js. Keep this file so the view can
  // reference it without a 404, and so future settings behavior has a home.
})();
