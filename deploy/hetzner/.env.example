# Copy to .env on the server, fill in the real values, never commit.
# `docker compose --env-file .env -f docker-compose.yml up -d --build`

# Your apex domain. Subdomains api.${DOMAIN} and admin.${DOMAIN} must
# already resolve to this VPS via A records.
DOMAIN=tilmezbus.com

# Postgres credentials — used by both the postgres container and the API.
POSTGRES_DB=tilmezbus
POSTGRES_USER=tilmezbus
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# JWT signing key — generate with: openssl rand -base64 64
JWT_KEY=REPLACE_WITH_64_RANDOM_BYTES_BASE64

# Seq log viewer admin password (any strong string).
# Generate with: openssl rand -base64 24 | tr -d '/+=' | head -c 24
SEQ_ADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# Firebase service-account JSON (the entire file on ONE line, as JSON).
# Generate with:
#   jq -c . firbase/smart-bus-firebase-firebase-adminsdk-fbsvc-XXX.json
# Then paste the output here. Single-line. Keep the surrounding quotes off.
FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"smart-bus-firebase","..."}

# Prelude (prelude.so) — OTP provider. Owns code generation, channel
# selection (SMS/voice/WhatsApp via dashboard dispatch rule), expiry,
# and attempt counting. Leave blank to use the dev console logger
# (master code 1234).
PRELUDE_API_KEY=sk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Transactional email — currently used by the admin forgot-password
# flow. SMTP relay credentials; configure for whatever mailbox sends
# from info@tilmezbus.com (Microsoft 365, Google Workspace, Mailgun,
# Postmark, etc.). Leave EMAIL_HOST blank to fall back to the dev
# console-log sender (the body shows up in `docker logs`).
EMAIL_HOST=smtp.office365.com
EMAIL_PORT=587
EMAIL_USER=info@tilmezbus.com
EMAIL_PASSWORD=REPLACE_WITH_SMTP_PASSWORD
EMAIL_FROM=info@tilmezbus.com
EMAIL_FROM_DISPLAY=TilmezBus
EMAIL_USE_SSL=true

# Base URL used to build links in transactional emails (the reset
# link sent by the forgot-password flow points at this host).
APP_BASE_URL=https://tilmezbus.com
