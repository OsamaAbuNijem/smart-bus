targets:
  $default:
    builders:
      json_serializable:
        options:
          # API uses camelCase for both responses and requests, so leave
          # field names as-is (Dart's natural camelCase).
          explicit_to_json: true
          create_to_json: true
          create_factory: true
          include_if_null: false
