@* Empty shell — contents rendered server-side and swapped in by JS via /Drivers/Form *@
<div class="modal-overlay" id="modal-driver">
  <div class="modal-box" id="driver-form-container"></div>
</div>
